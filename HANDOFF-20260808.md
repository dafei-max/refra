# Refra 项目移交说明（2026-08-08）

> 本文件是给下一任执行模型（Claude Code / Codex / 其他 LLM）的交接说明。
> 开始工作前请先完整阅读：`CLAUDE.md`、`HANDOFF.md`、`README.md`、`SNAPSHOT.md`，再读本文件。

## 1. 项目一句话

营销 KV 智能生成工具：两阶段生图链路（第一步文字版式图 → 第二步完整 KV），当前正按 Figma 设计稿迭代为「顶栏 + 首页生成 + React Flow 无限画布」的新交互形态。

## 2. 当前版本状态（2026-08-08）

- Git HEAD：`7fd197d`（`chore: untrack local vision helper`），工作区干净，远端 `origin/main` 已同步。
- 近期提交链：
  - `d63050c` 回滚基线（旧 UI + 完整生图链路，OSS/鉴权/压缩修复齐全）
  - `ef18ea0` 空提交（强制触发 Vercel 部署）
  - `e93d7a2` 本轮迭代：顶栏导航 + Invite 弹窗 + 首页布局 + React Flow 无限画布
  - `7fd197d` 将本地工具 `vision.cjs` 移出 Git
- 重要标签：
  - `v0.1.0-oss-stable`（fa14d4b）：第一个稳定回滚点
  - `backup/redesign-v2-20260807`（828beda）：被回滚掉的 Lovart 深色改造 + 项目模型，保留备用
- 线上：Vercel Production = `https://refra-brown.vercel.app`，当前已部署 `7fd197d`。

## 3. 线上环境（值不在仓库里，均在 Vercel 环境变量 / 本地 .env）

- Vercel 项目通过 GitHub 集成自动部署；`vercel.json` 使用 services 模式，入口 `server.mjs`，函数区域固定 `hnd1`（香港，连上海 OSS）。
- 必须的环境变量（Vercel Production + Preview 已配置）：
  - `OPENAI_API_KEY`（文本模型 gpt-5，图片模型 gpt-image-2）
  - `ALIYUN_OSS_REGION` / `ALIYUN_OSS_BUCKET` / `ALIYUN_OSS_ACCESS_KEY_ID` / `ALIYUN_OSS_ACCESS_KEY_SECRET`
  - `ADMIN_TOKEN`（全部写接口/生成接口的 Bearer 鉴权）
  - 可选：`OSS_SIGNED_URL_TTL`、`RATE_LIMIT_*`、`MAX_UPLOAD_BYTES` 等
- OSS：`refra-assets`（上海，Private 桶，阻止公共访问）。持久化内容：`outputs/`（成图）、`uploads/`（参考图/素材）、`data/assets.json`（资产索引）、`data/materials.json`、`data/style-presets.json`。
- 本地 `.env`（gitignored）：含 `DASHSCOPE_API_KEY` / `VISION_MODEL` 等，供 `vision.cjs` 识图脚本使用（千问 VL，按量付费）。
- Figma：设计文件 key `IJfVxDV4OsV5XWMNeW0wpo`（灵眸设计探索）；访问令牌由用户曾在对话中提供（`figd_` 开头），**不存仓库**，后续需要时向用户重新索取。相关节点：
  - 顶栏 `496-1981`、Invite 弹窗 `501-2648`、首页 `496-1500`/`496-1557`、发送按钮 `496-1604`、上传态 `504-3003`、填写态 `504-3078`、最近项目 `496-1606`、画布入口 `504-2707`、画布完成态 `465-5385`、节点工具条 `469-5387`。

## 4. 架构总览

- 后端：单体 `server.mjs`（Node 原生 HTTP，约 6600 行）。职责：静态资源、全部 API、两阶段生成链路（SSE）、素材/风格/资产/项目、鉴权限流、OSS 存储。
- 存储层：`services/storage-adapter.mjs`（fs / OSS 双后端；OSS 私有桶 + 短时签名 URL，默认 3600s；持久化只存 objectKey）。
- 图片解析：`services/image-source-resolver.mjs`（http/data/本地静态/运行时目录，中文与空格路径、MIME、大小、穿越防护）。
- 灵感搜索：`services/inspiration/`（Pinterest / Behance）。
- 前端：原生 JS SPA（`public/index.html` + `public/app.js` + `public/styles.css`）。
- 无限画布：React 19 + `@xyflow/react` v12（MIT），源码 `frontend/canvas.jsx`，esbuild 打包为 `public/canvas-app.js`（已提交产物，Vercel 无需构建）；样式 `public/canvas-flow.css`。

## 5. 关键文件地图

| 路径 | 说明 |
|---|---|
| `server.mjs` | 服务端全部逻辑（API、生成链路、静态服务） |
| `services/storage-adapter.mjs` | OSS/fs 存储适配、签名 URL |
| `services/image-source-resolver.mjs` | 参考图统一解析 |
| `public/index.html` | SPA 结构：顶栏、首页、项目/风格/灵感、画布挂载点、Invite 弹窗 |
| `public/app.js` | 首页交互、素材/风格/资产、Invite、send→画布入口 |
| `public/styles.css` | 全部样式（含本轮顶栏/首页/画布样式） |
| `frontend/canvas.jsx` | React Flow 画布 + 右侧对话 + 节点工具条 |
| `public/canvas-app.js` | 画布打包产物（`npm run build:canvas` 重新生成） |
| `tools/` | 素材导出/同步等脚本 |
| `test/` | node:test 测试（当前 23 个全过） |
| `vision.cjs` | 本地识图辅助（未跟踪，gitignored；配合 `.env` 的 DashScope key） |

## 6. 本轮迭代已完成内容（e93d7a2）

1. 顶栏：`side-rail` 移到顶部。左侧品牌 logo，中间主导航（生成/项目/风格/灵感，选中态深灰胶囊 `#161616`），右侧蓝色 `Invite` 按钮。
2. Invite 弹窗：显示 ADMIN_TOKEN + 复制按钮；背景 `rgba(0,0,0,0.5)` + `backdrop-filter: blur(100px)`；卡片 `#212121`，输入区 `#303030`。
3. 首页：居中标题「今天想创作什么？」（34px/48px/600）；`brief-top-row` 移入 `prompt-dock` 并做成句子式（填写后值变青 `#14E1B9`）；发送按钮为火花图标（无输入 50% 透明，有输入 100%）；上传缩略图条深色化；下方「最近项目」（与项目 tab 同数据源）与「灵感发现」（与灵感 tab 同数据源）。
4. 无限画布：点击 send 进入画布页；React Flow 画布（点阵背景、缩放/平移/小地图）；右侧对话区左边缘可拖拽调宽（280~640px）；SSE 生成过程中逐步添加「第一步版式图」「完整 KV」节点并连线；聊天发消息生成新变体；点击节点出现白色工具条：HD 放大 / 拆分图层 / 下载（拆分通过 `/api/assets/split`，标题/背景图层作为新节点入画布）。

## 7. 已知问题与限制（重要）

1. 画布对话区内的「风格预设 / 3:4 / 扩写 / 上传参考图」按钮目前是**占位**，未接真实控件；会话设置来自首页 dock（风格/尺寸/兜兜/Logo/搜索框），参考图来自首页已上传的 `referenceFiles`。
2. 画布会话**未持久化**：刷新页面节点与对话清空（成图本身已写入 OSS 资产索引）。需要时基于备份 tag `backup/redesign-v2-20260807` 中的项目模型（`0cd0653`）重新引入，或另建方案。
3. 「项目」tab 仍是旧资产列表样式（内容与首页「最近项目」同源），尚未统一为卡片。
4. 旧的内联生成流（`runStream`/`applyStage`/`generationStory`）代码仍在 `app.js` 中但首页提交已改走画布，属于可清理的死代码。
5. Vercel 平台对总请求体有约 850KB 实测上限（超限返回 503，不进入函数）；前端上传压缩预算为 600KB（`compressReferenceFile`）。素材/风格大图上传同样受限，后续建议 OSS 直传。
6. 生成限流：`/api/run`、`/api/run-stream` 默认 3 次/分钟（RATE_LIMIT_RUN_PER_MIN 可调）。
7. Vercel 部署偶发不随 push 触发：遇到「代码已推、生产未更新」时，推一个空提交强制触发（已有两次先例）。
8. `vision.cjs` 已 gitignore（本地保留）；如需入库需用户确认。

## 8. 后续任务建议（按优先级）

1. 用户浏览器实测画布主流程（send → 生成节点 → 拖拽调宽 → 节点工具条 → 聊天变体），修复反馈问题。
2. 把画布对话区占位按钮接成真实控件（风格/尺寸/扩写/上传），并让画布会话设置与首页设置联动。
3. 画布会话持久化（节点坐标、消息、成图引用），推荐复用/改编备份 tag 中的项目模型与 `/api/projects` 系列接口。
4. 「项目」tab 统一为卡片网格；「灵感」tab 视觉对齐首页灵感模块。
5. 清理旧内联生成流死代码。
6. 大图上传走 OSS 直传（预签名 PUT + 桶 CORS），绕开平台体限。

## 9. 开发约束与验证流程（务必遵守）

- **不可破坏两阶段生图链路与业务语义**（CLAUDE.md 硬约束）；不要改变 `services/image-source-resolver` 的安全规则。
- 不提交：密钥、`.env*`、`outputs/`、`uploads/`、`data/assets.json`、`data/projects.json`、`vision.cjs`、日志。
- 一个提交只解决一个明确问题；提交前 `git diff --check`。
- 每次修改后最低验证：
  ```bash
  git diff --check
  node --check server.mjs
  node --check public/app.js
  npm test
  ```
- 涉及图片解析时额外验证含中文/空格的 `/style/...` 路径；涉及部署时在 Production 上做一次 `/api/run-stream` 或等价冒烟。
- 修改前端画布后重新构建：`npm run build:canvas`（产物需提交）。

## 10. 本地运行 / 构建 / 部署

```bash
# 安装（含 devDependencies）
npm install

# 本地启动（fs 存储，写接口需带 ADMIN_TOKEN）
PORT=5196 ADMIN_TOKEN=testtoken STORAGE_BACKEND=fs node server.mjs
# 访问 http://localhost:5196

# 重新打包画布（改 frontend/canvas.jsx 后必须执行并提交产物）
npm run build:canvas

# 测试
npm test

# 部署：push main → Vercel 自动部署；未生效则推空提交：
git commit --allow-empty -m "chore: trigger vercel redeploy" && git push origin main
```

## 11. 凭据与安全（值在哪里）

- 生产密钥只在 Vercel 环境变量中；本地 `.env` 只含识图脚本用变量；聊天中曾出现过 ADMIN_TOKEN 与 Figma token（建议用户后续轮换）。
- 不要把任何 AccessKey / Token 写入代码、日志、Git 或本移交文档。

## 12. 给下一个模型的开场建议

1. 先跑 `git log --oneline -12`、`git status`、`npm test` 确认基线。
2. 用浏览器打开本地服务，对照 `HANDOFF.md` 与 Figma 节点，先复现「首页 → send → 画布生成」主流程。
3. 遇到视觉细节，可用 `vision.cjs <图片路径> '问题'` 让视觉模型描述设计稿/截图（需 `.env` 的 DashScope key）。
4. 大改动前先建分支（如 `codex/xxx`），不要直接动 main。
