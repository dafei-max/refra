{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "services": {
    "web": {
      "root": ".",
      "entrypoint": "server.mjs",
      "functions": {
        "server.mjs": {
          "regions": ["hnd1"],
          "includeFiles": "{Brief理解.md,设计判断.md,检索匹配.md,prompt生成.md,image/**,兜兜/**,uploads/**,data/**}"
        }
      }
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": {
        "service": "web"
      }
    }
  ]
}
